from setuptools import setup, find_packages

setup(name="models", packages=find_packages())
setup(name="oauth2", packages=find_packages())
setup(name="utils", packages=find_packages())
setup(name="schemas", packages=find_packages())
